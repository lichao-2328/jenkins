<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

  <!-- 密码加密器（演示用 NoOp） -->
  <beans:bean id="noopEncoder" class="org.springframework.security.crypto.password.NoOpPasswordEncoder"/>

  <!-- 自定义 UserDetailsService -->
  <authentication-manager>
    <authentication-provider ref="myAuthenticationProvider"/>
    <authentication-provider user-service-ref="myUserDetailsService">
        <password-encoder ref="noopEncoder"/>
    </authentication-provider>
</authentication-manager>

  <http auto-config="true" use-expressions="true">
    <!-- 静态资源和登录页放行 -->
    <intercept-url pattern="/static/**" access="permitAll"/>
    <intercept-url pattern="/login" access="permitAll"/>
    <intercept-url pattern="/captcha" access="permitAll"/>
    
    <!-- 其它请求需要认证 -->
    <intercept-url pattern="/**" access="isAuthenticated()"/>

    <!-- 登录配置 -->
    <form-login login-page="/login"
                login-processing-url="/do-login"
                authentication-details-source-ref="myAuthenticationDetailsSource"
                default-target-url="/login-success"
                authentication-failure-url="/login?error=true"
                username-parameter="username"
                password-parameter="password"
              />

    <!-- 注销配置 -->
    <logout logout-success-url="/login?logout=true"/>

    <!-- 禁用 CSRF，避免验证码请求被拦截 -->
    <csrf disabled="true"/>
  </http>
  <!-- 自定义 AuthenticationProvider：包含验证码校验 -->
<beans:bean id="myAuthenticationProvider" class="controller.login.MyAuthenticationProvider"/>

<!-- 自定义 DetailsSource：用于在登录时读取验证码 -->
<beans:bean id="myAuthenticationDetailsSource" class="controller.login.MyAuthenticationDetailsSource"/>

<!-- 用户信息加载 -->
<beans:bean id="myUserDetailsService" class="service.impl.MyUserDetailsService"/>

<!-- 使用自定义 provider -->
<authentication-manager>
    <authentication-provider ref="myAuthenticationProvider"/>
</authentication-manager>
  
</beans:beans>
